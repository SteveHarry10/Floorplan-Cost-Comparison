# Data Folder

GitHub Actions writes `floorplans-data.json` here after downloading and reading the SharePoint Excel workbook.

This package intentionally starts without that JSON file so there is no bundled cost data.
